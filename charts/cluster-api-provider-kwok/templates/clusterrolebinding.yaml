apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "cluster-api-provider-kwok.fullname" . }}-manager-rolebinding
  labels:
    {{- include "cluster-api-provider-kwok.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "cluster-api-provider-kwok.fullname" . }}-aggregated-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "cluster-api-provider-kwok.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
