---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  labels:
    cluster.x-k8s.io/provider: infrastructure-kwok
    cluster.x-k8s.io/v1beta1: v1alpha1
    cluster.x-k8s.io/v1beta2: v1alpha1
  name: kwokmachinepools.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: KwokMachinePool
    listKind: KwokMachinePoolList
    plural: kwokmachinepools
    singular: kwokmachinepool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KwokMachinePool is the Schema for the kwokmachinepools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KwokMachinePoolSpec defines the desired state of KwokMachinePool.
            properties:
              providerID:
                description: ProviderID is the unique identifier for this machine
                  pool.
                type: string
              providerIDList:
                description: ProviderIDList is the list of provider IDs for the machines
                  in the pool.
                items:
                  type: string
                type: array
              simulationConfig:
                description: SimulationConfig holds the configuration options for
                  changing the behavior of the simulation.
                properties:
                  reconcile:
                    description: Reconcile holds the configuration options for changing
                      the behavior of the reconciliation loop.
                    properties:
                      latency:
                        description: Latency is the amount of time to wait before
                          returning from the reconcile loop.
                        type: string
                    required:
                    - latency
                    type: object
                type: object
              template:
                description: Template contains the KwokMachine template for machines
                  in the pool.
                properties:
                  metadata:
                    description: Standard object's metadata.
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: Spec is the specification of the desired behavior
                      of the machine.
                    properties:
                      providerID:
                        description: ProviderID is the unique identifier as specified
                          by the cloud provider.
                        type: string
                      simulationConfig:
                        description: SimulationConfig holds the configuration options
                          for changing the behavior of the simulation.
                        properties:
                          reconcile:
                            description: Reconcile holds the configuration options
                              for changing the behavior of the reconciliation loop.
                            properties:
                              latency:
                                description: Latency is the amount of time to wait
                                  before returning from the reconcile loop.
                                type: string
                            required:
                            - latency
                            type: object
                        type: object
                    type: object
                type: object
            type: object
          status:
            description: KwokMachinePoolStatus defines the observed state of KwokMachinePool.
            properties:
              conditions:
                description: Conditions defines current service state of the KwokMachinePool.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              infrastructureMachineKind:
                description: |-
                  InfrastructureMachineKind is the kind of infrastructure machine
                  used by the MachinePool Machines pattern.
                type: string
              ready:
                default: false
                description: Ready is true when the pool infrastructure is ready.
                type: boolean
              replicas:
                description: Replicas is the number of replicas current in this pool.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
